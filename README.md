# Symfony 7.4 elearning website demo

# Setup
symfony new dnpm-website --webapp
cd dnpm-website
composer require --dev symfony/maker-bundle

symfony server:ca:install

symfony local:server:start

git comes with it, so https://www.toptal.com/developers/gitignore/api/visualstudiocode,symfony

bin/console debug:router

make HomeController with home.html.twig